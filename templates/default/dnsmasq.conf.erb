domain-needed
bogus-priv

listen-address=127.0.0.1
listen-address=<%= @hub_address %>
bind-interfaces

# external DNS servers (Google, OpenDNS, etc)
server=8.8.8.8
server=8.8.4.4
server=208.67.220.220
